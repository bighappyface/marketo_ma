<?php

/**
 * @file
 * Sends webform submissions to Marketo MA.
 */

/**
 * Implements hook_theme().
 */
function marketo_ma_webform_theme() {
  $info = [
    'webform_handler_marketoma_post_summary' => [
      'variables' => ['settings' => NULL, 'handler' => []],
    ],
  ];
  return $info;
}
